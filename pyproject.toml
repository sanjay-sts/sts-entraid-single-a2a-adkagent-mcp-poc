[project]
name = "identity-aware-agent"
version = "1.0.0"
description = "Identity-Aware AI Agent System with three-tier access control"
readme = "README.md"
requires-python = ">=3.10"

# Dependencies are managed via requirements.txt for this service-based project
# Use: uv pip install -r requirements.txt

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
