# FastMCP - MCP server with authentication support
fastmcp>=3.0.0

# Google ADK - Agent Development Kit
google-adk>=1.25.0
google-genai>=1.0.0

# LiteLLM - Multi-provider LLM support (for Claude, OpenAI, etc.)
litellm>=1.30.0

# AWS Bedrock support (required by LiteLLM for Bedrock models)
boto3>=1.34.0

# A2A SDK - Agent-to-Agent Protocol with streaming support
a2a-sdk[http-server]>=0.3.20

# Web framework
fastapi>=0.109.0
uvicorn>=0.27.0

# HTTP client
httpx>=0.26.0

# JWT token validation
PyJWT>=2.8.0
cryptography>=42.0.0

# Azure Identity (OBO token exchange for Graph API)
azure-identity>=1.19.0

# Environment variables
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-html>=4.0.0
pytest-timeout>=2.2.0
